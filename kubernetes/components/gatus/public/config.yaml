endpoints:
  - name: "${GATUS_NAME:=${APP}}"
    group: Public
    url: "https://${GATUS_DOMAIN:=${APP}.${DOMAIN_APP_APPS}}${GATUS_PATH:=/}"
    interval: 1m
    client:
      dns-resolver: tcp://1.1.1.1:53
    conditions:
      - "[STATUS] == ${GATUS_STATUS:=200}"
