---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: storage-config
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 5Gi
  storageClassName: ceph-block
  volumeName: storage-config
